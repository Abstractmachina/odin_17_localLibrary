extends layout

block content
    h1 Delete #{bookinstance.book.title}

    p Do you really want to delete this book instance?

    form(method='POST' action='')
        div.form-group
            input#bookinstance_id.form-control(type='hidden',name='bookinstanceId', required='true', value=bookinstance._id )

            button.btn.btn-primary(type='submit') Delete